import * as React from 'react';
import { View, Text } from 'react-native';

export default function PreviousLocations(){
    return(
        <View>
            <Text style={{fontSize:50}}>Previous Locations SCREEN</Text>
        </View>
    )
}